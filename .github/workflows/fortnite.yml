name: Fortnite Updates

on:
  workflow_dispatch:
    inputs:
      force_latest:
        description: 'Enviar a novidade mais recente como teste'
        required: false
        default: false
        type: boolean

  schedule:
    - cron: '23 13 * * *'
    - cron: '23 21 * * *'

permissions:
  contents: write

concurrency:
  group: discord-automation-fortnite
  cancel-in-progress: false

jobs:
  publicar:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4

      - name: Configurar PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: curl, mbstring, dom, simplexml
          coverage: none

      - name: Verificar Fortnite
        env:
          WEBHOOK_FORTNITE: ${{ secrets.WEBHOOK_FORTNITE }}
          FORCE_LATEST: ${{ github.event_name == 'workflow_dispatch' && inputs.force_latest || false }}
        run: php scripts/fortnite.php

      - name: Persistir estado
        if: success()
        run: |
          if [ -z "$(git status --porcelain -- .state/fortnite.json)" ]; then
            echo 'Nenhuma alteração de estado para registrar.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .state/fortnite.json
          git commit -m 'chore: update fortnite state'
          git pull --rebase origin main
          git push
